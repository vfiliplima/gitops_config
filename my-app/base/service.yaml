apiVersion: v1
kind: Service
metadata:
  name: my-cmatrix-service
  labels:
    app: my-cmatrix
spec:
  type: NodePort
  selector:
    app: my-cmatrix
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP