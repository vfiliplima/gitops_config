resources:
  - ../../base

nameSuffix: -dev

labels:
  env: dev

patches:
  - target:
      kind: Deployment
      name: nginx
    patch: |
      - op: replace 
      path: /spec/replicas
      value: 2