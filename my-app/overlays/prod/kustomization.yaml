resources:
  - ../../base

nameSuffix: -prod

commonLabels:
  env: prod

patches:
  - target:
      kind: Deployment
      name: nginx
    patch: |
      - op: replace 
      path: /spec/replicas
      value: 5