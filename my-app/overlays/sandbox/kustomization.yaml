resources:
  - ../../base

nameSuffix: -sandbox

labels:
  env: sandbox

patches:
  - target:
      kind: Deployment
      name: nginx
    patch: |
      - op: replace 
      path: /spec/replicas
      value: 1