resources:
  - ../../base

nameSuffix: -sandbox

patches:
  - target:
      kind: Deployment
      name: nginx-demo-sandbox
    patch: |
      - op: replace 
        path: /spec/replicas
        value: 1